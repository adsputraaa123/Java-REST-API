README — How to run this Spring Boot project

Prerequisites:
- Java (11+)
- Maven

Quick run:
- Command for running the spring boot app:
    mvn spring-boot:run
- Command for running the spring boot test:
    mvn test

Notes:
- Default server URL: http://localhost:8080/api/tasks
